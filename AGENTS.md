# TaskManager Agent Guide

Use hiro-task-manager-cli skill to interact with the TaskManager API.
The only override is to use "--profile dev" in all commands unless told otherwise.
When working with the dev profile, you need to start the dev server if not already running.

```bash
hirotm server status --profile dev
hirotm server start --profile dev
```


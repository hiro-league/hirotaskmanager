1. good idea, let the cli explicitly identify itself as cli as you suggested, tokens later.

2. boards list will show boards and their access included.
if cli tries to show one of them, gets 403

3. migration: all none.default: none. add hint in web app when editing a board. show a clear no access icon around board, return message to cli when it is shut off, telling them to ask user to give access. better late than sorry.
4.  read means anything that can list or show, but no edits, no updates, no creates.

5. ok

6. yes, remove the task groups and priorities buttons and place them in the menu, yes simply navigation, you got it.

7. plain text, not necessary to touch search now, we will remember later.
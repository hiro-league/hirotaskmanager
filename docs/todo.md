1. ~~settings entry sidebar footer. settings route.~~ → `/settings`, footer link + collapsed gear icon.
2. ~~do advanced only.~~ → Settings page: global `create_board` + per-board granular `cliPolicy` toggles (no tri-state presets in Settings).

/**
 * Authoritative default ports for both runtimes. Imported by CLI (Node), server,
 * and browser bundle — keep this file free of Node-only imports so it is safe
 * for the Vite client build.
 */
export const INSTALLED_DEFAULT_PORT = 3001;
export const DEV_DEFAULT_PORT = 3002;
